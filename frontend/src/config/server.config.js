const SERVER_URL = ""; // Ruta relativa
const PORT = ""; // No es necesario especificar el puerto en producción
const SERVER_API_URL = "/api"; // Ruta relativa para la API

export default { SERVER_URL, PORT, SERVER_API_URL };
